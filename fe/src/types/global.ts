export type ErrorLabel = {
    password?: String
    email?: String
    userName?: String
    passwordConfirm?: String
}

export type QueryValue = {
    userId: String
    status?: Boolean
    year?: Number
    month?: Number
}